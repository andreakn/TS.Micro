@model Timesheet.Micro.Models.Viewmodels.ProjectEditModel

@{
    ViewBag.Title = "title";
}


<h2>Rediger @Model.Project.Name</h2>
<div>
    @using (Html.BeginForm())
    {
        @Html.Partial("_Form", Model);

        <input type="submit" class="btn" value="Lagre" />
    }
</div>